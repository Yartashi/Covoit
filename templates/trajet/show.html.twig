{% extends 'base.html.twig' %}

{% block body %}
    <h1>Détails d'un trajet 
    <div class="media" style="margin-top: 60px;">
        <div class="media-body">
            <div class="row">
                <div class="col-sm-10">
                    <h3 class="media-heading"><strong>Départ :{{ trajet.villeDep }} , Arrivé : {{trajet.villeDest}} </strong></h3>
                </div>

                <div class="col-sm-2">
                    <i class="pull-right">Publié le {{ trajet.dateDep|date('m/d/Y') }}</i>
                </div>
            </div>


            <p>{{ trajet.description|nl2br }}</p>

            <p>
                <strong>Nombre de places maximum  : </strong> {{ trajet.nombrePlacesMax }}
            </p>
            <p>
                <strong>Les commentaires : <strong>
                <table class="table">
                <thead>
                <tr>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Email</th>
                    <th>Téléphone</th>
                </tr>
                </thead>
                <tbody>
                {% for commentaire in trajet.commentaires %}
                    <tr>
                        <td><a href="#">{{ commentaire.utilisateur }}</a></td>
                        <td>{{ commentaire.dateComm }}</td>
                        <td>{{ commentaire.messageCom }}</td>
                    </tr>
                {% endfor %}
                </tbody>
                </table>
            </p>
            <div class="row">
                <div class="col-sm-12 text-right">
                    <a class="btn btn-default" href="#">
                        <span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span>
                        Liste des trajets
                    </a>

                    <a class="btn btn-primary" href="#">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                        Éditer
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}